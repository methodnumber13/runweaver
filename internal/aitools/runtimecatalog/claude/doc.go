// Package claude provides runtimecatalog metadata for Claude Code.
//
// The adapter describes Claude Code's generated profile path, managed metadata
// directories, runtime capabilities, and prompt guidance. Command execution and
// filesystem discovery live in the core runtime adapter package.
package claude
